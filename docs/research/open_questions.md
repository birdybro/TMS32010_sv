# Open architecture questions

Open questions are not implementation permissions. Provisional choices require
a targeted test and must retain their confidence label.

| ID | Question | Status / competing hypotheses | Evidence needed | Hard Drivin' impact |
|---|---|---|---|---|
| OQ-001 | Can external cycles be lengthened on a part with no READY pin? | UNKNOWN: clock-input pause may be legal, or memories must always meet fixed AC limits | TI clock-generator and AC sections; hardware measurement if silent | program RAM and wrapper clock-enable design |
| OQ-002 | What is observed for original-TMS32010 data addresses `0x90`–`0xff`? | UNKNOWN: alias, open/internal value, or undocumented decode | original data-sheet decode detail or physical chip | likely low unless software accesses it |
| OQ-003 | What value does `SST` produce in reserved bit 1? | BLOCKED: SPRU001B Figure 2-7 marks bit 1 “don't care,” its LST page and SPRU002B show one, while SPRU013 shows zero; bits 12:9 and 7:2 are consistently one | original-part erratum or a physical TMS32010 store-status measurement across relevant mask revisions | possible context-save code that inspects the complete saved word |
| OQ-004 | What is the exact interrupt-recognition and vector-fetch cycle trace? | PARTIALLY RESOLVED_PRIMARY: SPRU001B Figure 2-12 establishes fetch N, fetch N+1, dummy fetch N+2, and fetch vector 2 beside execute N, execute N+1, dummy, and execute 2. Directed RTL reproduces the external read order and entry state. A 32-case matrix exhausts active-low arrival at every represented machine cycle of the 11 supported two-word control-flow families, IN, OUT, TBLR, and TBLW, with completion/protected-instruction/dummy/vector and logical-bus assertions. A four-phase native test proves the digital wrapper samples a held-low request only at an enabled falling boundary, including a phase-2 stall. The partial core still maps effects to fetch-sample boundaries instead of representing the complete overlapped execute timeline; physical setup/synchronizer behavior and unsupported CALA/RET/PUSH/POP cycles remain unqualified | distinct fetch/execute pipeline state and flush ownership; physical trace only if setup/synchronizer behavior remains externally ambiguous after a proper CDC wrapper | low on A044427 Rev A because board INT is pulled inactive-high |
| OQ-005 | Is Hard Drivin' TMS32010 `INT` tied inactive, and what net is `PR1`? | RESOLVED_PRIMARY for production Rev-A drawing A044427: DSP pin 5 connects to `PR1`; sheet 1 pulls `PR1` inactive-high through `R26` (1 kΩ), no loaded active driver appears anywhere in the ten-sheet drawing, and the only switching option on sheet 7 is marked `NOT LOADED`. Pinned MAME independently configures BIO but no DSP interrupt source. Other board revisions have not been audited | audit another physical board revision only before claiming equivalence beyond A044427 Rev A | Rev-A wrapper defaults INT high/inactive while the generic core retains an external input |
| OQ-006 | Which edge completes reset and starts the first address-0 fetch? | RESOLVED_PRIMARY: recognized reset changes are synchronized to falling CLKOUT; PC/address clears after the next complete cycle, and release resumes after one complete cycle with address 0 then 1 | Appendix A reset timing, data-sheet p. 19 (PDF p. 375); directed RTL phase test passes | reset/halt release trace |
| OQ-007 | What are exact taken/untaken branch prefetch, execute-interval, and address traces? | PARTIALLY RESOLVED: TI establishes overlap, two words/two cycles, the following target operand, and ordinary program fetches, but no dedicated original-part branch pin waveform has been located. Exact `B`, `BANZ`, `BV`, `BIOZ`, and the six accumulator-conditional branches now have INFERRED explicit-pipeline mappings: opcode-prefetch completion enters execute ownership; PC+1 operand fetch is execution cycle 1; selected-instruction fetch is execution cycle 2; the branch retires as that word enters the execute slot. B always selects target. BANZ uses the old selected nine-bit counter to select target/fallthrough and defers its modulo-512 decrement until retirement. The accumulator family selects from the unchanged full 32-bit ACC and preserves it. BV selects from old sticky OV and clears it only at taken retirement. BIOZ samples raw active-low BIO at operand completion and retains the resulting decision, not an opcode-time pin latch, through the selected fetch. Directed tests cover every integrated condition, target/fallthrough selection, selected-fetch stalls, no early fetched-instruction effect, and malformed-operand parking. CALL retains legacy opcode/operand/next-address bus tests but not explicit execute-slot ownership; its combined commit mapping must not be called VERIFIED_PRIMARY. BIOZ's raw active-low sampling fact and all numeric totals remain primary-verified. CALA and RET are functionally/model-qualified as exact words `0x7f8c`/`0x7f8d` with documented state effects and two-cycle totals, but their second external cycles remain UNKNOWN | original TI branch/return control timing, development-system traces, or physical logic-analyzer captures; extend the explicit pipeline one family at a time under targeted tests | program trace alignment and interrupt-return timing |
| OQ-008 | Does any mask revision materially differ? | UNKNOWN | TI errata/mask notices or measured devices | release qualification |
| OQ-009 | Is a lawful, automatable modern TMS32010 assembler available? | RESEARCHING; no qualified candidate yet | license/source/encoding audit | local test-program workflow |
| OQ-010 | What does an indirect control field do when increment and decrement bits are both one? | UNKNOWN: TI defines each bit separately but offers no syntax, example, or combined result; current decode rejects it | explicit TI reserved-encoding statement or physical opcode test | malformed code only; ordinary Hard Drivin' binaries are unlikely to emit it |
| OQ-011 | Does original-TMS32010 `ADDH` affect `OV`/`OVM`, and if saturated does its low accumulator half change? | UNKNOWN: original and SPRU013 pages omit status behavior; C14/E14 says `OV`/`OVM` apply while the low half is unaffected; MAME implements high-half-only saturation as an inference | original-part erratum, authoritative microarchitecture detail, or physical TMS32010 boundary tests | unknown until Hard Drivin' opcode/OVM usage is inspected |
| OQ-012 | Does physical reset preserve every state element not named by TI, including `OV`, `ACC`, ARs, ARP, and DP? | UNKNOWN: TI explicitly changes PC/interrupt state and preserves OVM but does not characterize the other registers; current RTL assigns them no reset value and therefore retains prior FPGA state | explicit TI reset-state statement, die/reset-network evidence, or before/after physical-chip tests | software may inspect retained arithmetic/address state after host reset |
| OQ-013 | Does original-TMS32010 `ABS` set sticky `OV` when negating `0x80000000`? | UNKNOWN: original-part instruction pages define the OVM-dependent result but omit the OV side effect; the C14/E14 variant explicitly says ABS affects OV, while pinned MAME leaves OV unchanged | original-part erratum or microarchitecture detail, or a physical test beginning with OV clear and observing status through `SST` after the boundary case | relevant only if code takes this boundary absolute value and later tests or saves OV |
| OQ-014 | What does `DMOV`/`LTD` do when the next-higher destination is outside the original 144-word RAM, especially source `0x8f` and destination `0x90`? | UNKNOWN: TI defines an unconditional copy to `dma+1` but does not define absent-location decode, aliasing, write suppression, or whether the other parallel LTD effects complete | original data-RAM decode detail, erratum, or a physical-chip program that observes T/ACC/status after the boundary move | likely low unless software places a delay line at the final implemented word |
| OQ-015 | After indirect `LST`, does an encoded next-ARP field or data-word bit 8 determine final `ARP`? | PROVISIONAL: original TMS32010 guides expose the optional field while also saying the memory word loads ARP, but do not state precedence; later TI SPRU012 explicitly says the field is ignored, and pinned MAME independently agrees | original-part erratum, microarchitecture description, or physical TMS32010 test with conflicting values | context restore can select the wrong AR if the provisional precedence is wrong |
| OQ-016 | What exact external program-address and `MEN` sequence occurs during the second cycle of single-word `PUSH` and `POP`? | RESEARCHING: original instruction pages and Table 3-2 establish one word/two cycles and the specified stack/ACC effects, but no located waveform identifies whether the next word is prefetched, held, or fetched only after an inactive extra cycle | original TI pipeline/control timing diagram, development-system cycle trace, or physical logic-analyzer capture around minimal PUSH/POP programs | blocks honest native-phase RTL qualification even though model/tool and stack-state research can proceed |
| OQ-017 | At what subphase does `SUBC` make its ACC result available, and what deterministic silicon behavior occurs if the immediately following instruction violates TI's rule that it cannot use ACC? | UNKNOWN: both original TMS32010 guides state the scheduling prohibition but do not describe the violating sequence; the qualified implementation updates ACC at its instruction boundary and tests only TI-compliant sequences with an ACC-free following instruction | original pipeline/microcode documentation or a physical trace comparing a dependent instruction with an intervening NOP | may change deliberately invalid code and the exact interrupt/result-visibility boundary; legal Hard Drivin' code must be audited for the required gap |
| OQ-018 | Which `SUBC` arithmetic stage drives sticky `OV` on the original TMS32010: the intermediate subtraction, the final left shift, or both? | PROVISIONAL: SPRU001B defines both stages but omits an instruction-specific flag rule; its generic accumulator rule says arithmetic overflow sets OV, later first-generation TI documentation says SUBC affects OV but ignores OVM, and pinned MAME appears intended to test intermediate subtraction overflow while its current expression cannot set the flag | original-part erratum/microarchitecture detail or physical tests that separately overflow only the intermediate subtraction or only the final shift | affects status saves and BV after out-of-domain SUBC operands; normal documented positive divide use does not exercise these boundary cases |
| OQ-019 | If `DINT` is the already-pipelined instruction immediately before an otherwise ready interrupt acknowledge, does its INTM write cancel entry or does the internal interrupt processor complete entry? | PROVISIONAL: current model/RTL cancels entry and retains the pending request. Figure 2-11 gates interrupt-active with the mode register and pinned MAME checks INTM before service, but the original prose does not explicitly order the DINT write against an already active IIP sequence | original-part microarchitecture/erratum wording or a physical test that asserts INT during instruction N and executes DINT as N+1 | low for normal handlers; relevant to deliberately masking at the last recognition boundary |
