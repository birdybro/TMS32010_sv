# Open architecture questions

Open questions are not implementation permissions. Provisional choices require
a targeted test and must retain their confidence label.

| ID | Question | Status / competing hypotheses | Evidence needed | Hard Drivin' impact |
|---|---|---|---|---|
| OQ-001 | Can external cycles be lengthened on a part with no READY pin? | RESOLVED_PRIMARY_LIMITED: no READY/WAIT input exists. SPRU001B requires the TMS32010-20 external master clock to remain within 48.78–150 ns and each pulse within 47.5–52.5% of its period; `CLKOUT` is four input periods, yielding a 195.12–600 ns physical machine-cycle envelope. Bounded slowing inside that envelope is supported, but an arbitrary/indefinite phase pause is outside specified conditions. TI does not separately characterize dynamic modulation between otherwise conforming periods. The tested FPGA `clock_enable_i` hold remains platform-only | closed by SPRU001B §2.12 and Appendix A Clock Characteristics and Timing, printed pp. 2-20 and data-sheet pp. 10–11 (PDF pp. 44 and 366–367); physical testing only if per-cycle modulation matters | a pin-compatible adapter must meet the fixed envelope; a MiSTer emulation may pause internal modeled phases without claiming physical compatibility |
| OQ-002 | What is observed for original-TMS32010 data addresses `0x90`–`0xff`? | RESEARCHING/CONFLICT (`SC-041`): production TI material proves exactly 144 implemented words at `0x00`-`0x8f` while direct/indirect formation can present every eight-bit address, but assigns no absent-select result. The related patent's row/column counts are internally inconsistent. Pinned MAME leaves the range unmapped; pinned IKA allocates 256 initialized words. Current model/RTL trap and diagnostic zero are fail-closed policies only | original production decoder/erratum, decap evidence that reaches RAM selection, or the ordered original-NMOS read-only controlled-history and ascending/descending unique-sentinel captures in `docs/research/ram_invalid_decode_experiment.md`; fixtures assign no expected absent value or alias | likely low unless software accesses it; current Hard Drivin' program qualification must still reject rather than invent such accesses |
| OQ-003 | What value does `SST` produce in reserved bit 1? | RESOLVED_CORROBORATED: bits 12:9 and 7:2 are unambiguously stored as ones across the original overview, LST, and SST figures; only bit 1 conflicts. SPRU001B Figure 2-7 marks bit 1 “don't care,” but both original instruction pages draw it as one. SPRU013 says reserved status bits read as ones and its worked `0x5efe` sets bit 1, while its Figure 3-13 and LST input drawing show zero and its SST drawing calls the position reserved. LST ignores bit 1 in every case. Pinned MAME independently forces it high before SST. The implementation stores one while documentation warns software that the position remains reserved; this is not a physical-mask measurement | original-part erratum or physical TMS32010 SST measurement only to upgrade confidence beyond CORROBORATED | possible context-save code that inspects the complete saved word |
| OQ-004 | What is the exact interrupt-recognition and vector-fetch cycle trace? | PARTIALLY RESOLVED_PRIMARY/CONFLICT (`SC-039`): SPRU001B Figure 2-12 establishes fetch N, fetch N+1, dummy fetch N+2, and fetch vector 2 beside execute N, execute N+1, dummy, and execute 2. Directed explicit-pipeline RTL retires one protected word while discarding N+2, performs entry with an empty execute slot while capturing vector 2, and executes the vector only in the following interval; stalls prove no early effects. MPY/MPYK protected-slot tests retain protection through one additional instruction, checking signed results, internal-read versus program-only bus shape, stalls, post-following dummy/return-PC ownership, and vector execution. Matching 32-case core and explicit-pipeline matrices exhaust active-low arrival at every represented execution interval of the 11 supported two-word control-flow families, IN, OUT, TBLR, and TBLW, with family-specific bus, completion, protected-instruction, dummy, stack, acknowledge, and vector assertions. Four additional explicit cases cover arrival in both CALA and RET intervals, no midinstruction stack effect, selected-target completion, one protected retirement, dummy/vector ownership, and entry stacking; their addresses retain ADR-0003 CORROBORATED-RET/INFERRED-CALA confidence. A four-phase native test proves the digital wrapper samples a held-low request only at an enabled falling boundary, including a phase-2 stall. Later mixed-family SPRU013 Figure 3-20 instead dummy-fetches N+1 and executes no protected word; it cannot silently override original production timing. Both guides require or recommend external CLKOUT-clocked conditioning for asynchronous NMOS input despite the original simplified internal Sync FF. Physical setup/conditioning behavior, provisional DINT cancellation ordering, PUSH/POP cycles, and physical confirmation of ADR-0003 remain unqualified | physical trace if setup/synchronizer behavior remains externally ambiguous after a proper CDC wrapper; primary or hardware evidence for DINT ordering and unsupported instruction cycles | low on A044427 Rev A because board INT is pulled inactive-high |
| OQ-005 | Is Hard Drivin' TMS32010 `INT` tied inactive, and what net is `PR1`? | RESOLVED_PRIMARY for production Rev-A drawing A044427: DSP pin 5 connects to `PR1`; sheet 1 pulls `PR1` inactive-high through `R26` (1 kΩ), no loaded active driver appears anywhere in the ten-sheet drawing, and the only switching option on sheet 7 is marked `NOT LOADED`. Pinned MAME independently configures BIO but no DSP interrupt source. Other board revisions have not been audited | audit another physical board revision only before claiming equivalence beyond A044427 Rev A | Rev-A wrapper defaults INT high/inactive while the generic core retains an external input |
| OQ-006 | Which edge completes reset and starts the first address-0 fetch? | RESOLVED_PRIMARY: recognized reset changes are synchronized to falling CLKOUT; PC/address clears after the next complete cycle, and release resumes after one complete cycle with address 0 then 1 | Appendix A reset timing, data-sheet p. 19 (PDF p. 375); directed RTL phase test passes | reset/halt release trace |
| OQ-007 | What are exact taken/untaken branch prefetch, execute-interval, and address traces? | PARTIALLY RESOLVED, CALA INFERRED/RET CORROBORATED: TI establishes overlap, two words/two cycles, the following target operand, and ordinary program fetches, but no dedicated original-part branch/call pin waveform has been located. Exact `B`, `BANZ`, `BV`, `BIOZ`, `CALL`, and the six accumulator-conditional branches now have INFERRED explicit-pipeline mappings: opcode-prefetch completion enters execute ownership; PC+1 operand fetch is execution cycle 1; selected-instruction fetch is execution cycle 2; the branch/call retires as that word enters the execute slot. B always selects target. BANZ uses the old selected nine-bit counter to select target/fallthrough and defers its modulo-512 decrement until retirement. The accumulator family selects from the unchanged full 32-bit ACC and preserves it. BV selects from old sticky OV and clears it only at taken retirement. BIOZ samples raw active-low BIO at operand completion and retains the resulting decision, not an opcode-time pin latch, through the selected fetch. CALL selects the target unconditionally and pushes opcode-PC+2 only at selected-target capture. CALA/RET are exact one-word/two-cycle operations, neither uses the `WE`/`DEN` exceptions to TI's every-cycle `/MEN` rule, and the TBL sequence demonstrates a discarded sequential fetch before a one-word redirect. ADR-0003 maps their cycle 1 to a discarded `PC+1` read and cycle 2 to the accumulator/old-TOS target fetch. A contemporary TI patent explicitly describes that sequence for RET in a related embodiment, raising RET ownership to CORROBORATED while CALA remains INFERRED; it is not an original-part production specification. The explicit pipeline implements the reversible mapping; directed tests cover both stalls, nonexecution, target capture, retirement-only stack effects, differential state/cycles, and all four interrupt-arrival intervals. The legacy retirement-mapped wrapper rejects the words rather than imply this sequence. IKA32010's idle first interval conflicts with the primary `/MEN` rule; original-part target-repeat remains a competing hypothesis under `SC-037`. These mappings must not be called VERIFIED_PRIMARY | original TI production-device branch/return control timing, development-system traces, or physical logic-analyzer captures; replace ADR-0003 if stronger evidence disagrees | program trace alignment and interrupt-return timing; exact original-pin qualification remains open |
| OQ-008 | Does any mask revision materially differ? | RESEARCHING/NO REVISION MAP (`SC-043`): pinned TI artifacts establish October-1985, February-1986, January-1987, and May-1989 data-sheet revisions plus changing NMOS speed-grade/current-product lists, but none identifies a silicon mask or assigns behavioral differences. TI routed current/new-device specification updates through a dial-up BBS whose authenticated TMS32010 notice archive has not been located. Package symbolization exposes tracking/date and lot fields without a published decoder. Later C10/C15/C17 devices remain variant evidence only | authenticated TI erratum, product-change/mask notice, or repeated original-NMOS captures with full raw package/date/lot provenance under `docs/research/device_revision_audit.md`; matching specimens establish only their measured scope unless a mask map is independently proved | release claims must name tested specimens and cannot assume mask invariance; Rev-A Hard Drivin' board behavior still requires its populated device marking or multiple representative original-NMOS captures |
| OQ-009 | Is a lawful, automatable modern TMS32010 assembler available? | RESOLVED_PROJECT_LOCAL: no external candidate was qualified, so the repository now supplies an MIT-licensed deterministic assembler and database-driven disassembler covering all sixty documented mnemonics, directives, diagnostics, fixed fixtures, and lossless round trips. The FIR and Hard Drivin' synthetic programs exercise the complete source-to-model workflow | reopen only if compatibility with a historical external object format or macro language becomes necessary | local test-program workflow is unblocked; proprietary game ROMs remain outside the repository |
| OQ-010 | What does an original TMS32010 indirect control field do when increment and decrement bits are both one? | RESEARCHING/CONFLICT (`SC-040`): original-part pages define each bit and only the three normal source forms, but neither call the combination reserved nor assign a result. A later TMS320C1x reference card says the bits cannot both be one. Pinned MAME and IKA nevertheless both model a forced word as no net AR update. Current model/RTL reject all 372 words before effects as a fail-closed policy, not a physical trap claim. A strict capture classifier now retains all three complete priority candidates, arbitrary other sequences, and three partial noncompletion stages without promoting any synthetic result | original-part erratum/control evidence or the stable original-NMOS two-boundary raw-word capture in `docs/research/simultaneous_ar_update_experiment.md`; repository fixture has no expected silicon sequence, and even `review_ready` requires raw physical-evidence review | malformed or deliberately raw code only; ordinary Hard Drivin' binaries are unlikely to emit it |
| OQ-011 | Does original-TMS32010 `ADDH` affect `OV`/`OVM`, and if saturated does its low accumulator half change? | RESOLVED_CORROBORATED: SPRU013's instruction-description contract says each Execution block lists affected status and mode behavior, but the original-family ADDH block lists neither. The C14/E14 variant explicitly adds both, and pinned MAME says it intentionally inferred overflow from newer generations despite the manual's omission. IKA32010 independently routes ADDH through a general saturating ALU, but disagrees with MAME on low-half saturation and therefore supplies no silicon resolution. The original-part implementation performs modulo high-half addition, always preserves ACC[15:0] and prior OV, and ignores OVM; this remains unmeasured | original-part erratum or physical TMS32010 boundary tests only to upgrade confidence beyond CORROBORATED | relevant if the sound program uses ADDH across a signed high-half boundary and then saves/tests OV or enables OVM |
| OQ-012 | Does physical reset preserve every state element not named by TI, including ACC, T, P, ARs, ARP, DP, stack, and OV? | RESEARCHING/CORROBORATED EVM (`SC-042`): production SPRU001B explicitly changes PC/interrupt state and preserves OVM but does not characterize the other registers. Contemporary TI EVM commands say warm RESET saves all registers except PC, while the same manual warns that the uncontrolled halt clears registers and may corrupt memory without publishing save ordering. MAME and IKA use conflicting reset policies. Current model/RTL retain all unlisted state as a reversible PROVISIONAL choice | original reset-network/erratum evidence or matching set- and clear-pattern original-NMOS captures from `docs/research/reset_retention_experiment.md`; each fixture records its state before reset, reconstructs destructively observed state, and selects the after path only with external BIO | software may inspect retained arithmetic/address state after host reset; the generic core must not substitute deterministic FPGA initialization |
| OQ-013 | Does original-TMS32010 `ABS` set sticky `OV` when negating `0x80000000`? | RESOLVED_CORROBORATED: SPRU013 §4.3 says each instruction's Execution block lists affected status bits; its TMS32010-family ABS page lists none, while the C14/E14 variant page explicitly adds `Affects OV`. Pinned MAME independently preserves OV. The original TMS32010 implementation therefore preserves prior OV for every ABS input; this is not a physical-silicon measurement | physical TMS32010 boundary test or original-part erratum only to upgrade confidence beyond CORROBORATED | relevant only if code takes this boundary absolute value and later tests or saves OV |
| OQ-014 | What does `DMOV`/`LTD` do when the next-higher destination is outside the original 144-word RAM, especially source `0x8f` and destination `0x90`? | RESEARCHING/CONFLICT (`SC-038`): TI defines an unconditional copy to `dma+1` but not absent-location decode, aliasing, write suppression, or whether LTD's parallel effects complete. SPRU001B's isolated `128-144` table conflicts with the consistent 144-word/`128-143` sources. A related TI patent explains ordinary adjacent-column movement but gives internally inconsistent row/column capacity and no array-edge outcome. Pinned MAME maps only through `0x8f` then issues the `+1` write; IKA instead allocates 256 words | original data-RAM decode/erratum, or the original-NMOS clear/scan/register capture defined in `docs/research/ram_boundary_experiment.md`; current synthetic images are stable and require physical execution | likely low unless software places a delay line at the final implemented word; current model/RTL trap-before-effects remains explicitly provisional |
| OQ-015 | After indirect `LST`, does an encoded next-ARP field or data-word bit 8 determine final `ARP`? | RESEARCHING/CONFLICT (`SC-009`): original TMS32010/first-generation guides say the memory word restores ARP, but their `LARP 0; LST *,1` example also says ARP becomes one without specifying word bit 8; ordinary indirect prose says the encoded field loads after execution and gives no exception. Later TI SPRU012 and pinned MAME implement memory-word precedence; pinned IKA implements encoded-field precedence. Current model/RTL retain memory-wins as a reversible PROVISIONAL policy. The strict `tools.trace.lst_arp_capture` workflow checks the exact image and three OUT anchors, preserves memory/encoded/both mixed/other sequences, requires 32 stable runs plus raw/photo provenance, and resolves no candidate unless both directions agree. Synthetic traces are not device evidence | original-part erratum/control description or the stable two-direction original-NMOS marker capture in `docs/research/lst_arp_precedence_experiment.md`; repository fixture has no expected silicon sequence and `review_ready` cannot promote confidence without raw-capture review | context restore can select the wrong AR if the provisional precedence is wrong |
| OQ-016 | What exact external program-address and `MEN` sequence occurs during the two execution cycles of single-word `PUSH` and `POP`? | RESEARCHING, NARROWED_PRIMARY: original instruction pages establish one word/two cycles, `(PC)+1 -> PC`, and exact stack/ACC effects. The general original-part pin contract says `MEN` is active on every machine cycle except when `WE` or `DEN` is active; PUSH/POP assert neither, so a completely inactive extra interval conflicts with that rule. Program memory is always addressed by PC, but no located primary waveform says whether `N+1` is repeated/discarded or whether another prefetch address appears. Contemporary TI patent US4577282A independently restates the every-state external-read rule, but its disclosed instruction table omits accumulator PUSH/POP and contains only subroutine-stack push/pop control; it therefore strengthens the constraint without choosing an address hypothesis. TI's original-device EVM rejects a breakpoint at the word immediately after PUSH/POP, and its breakpoint RAM is indexed directly from the TMS32010 program-address bus. This corroborates `N+1` visibility in the multicycle context but supplies no `MEN` phase, repeat count, or later address, so all three hypotheses survive. Pinned IKA32010 holds PC and requests no bus transaction in its first microcycle, then reads the opcode in its second; its first interval conflicts with the primary `MEN` rule and remains only a secondary hypothesis (`SC-018`). A live ROM-free MAME TMS320C10 trace now matches ten model state steps across a combined PUSH/POP/CALA/RET fixture, but MAME exposes no `/MEN` or per-cycle program address, so this adds functional corroboration without choosing a bus hypothesis. TI SPRU011 independently confirms that contemporary XDS/22/Kontron tooling could sample machine cycles and clock-qualified external fetches but includes no instruction trace. TI's 1982 software simulator distinguishes instruction acquisition from program-ROM reads and counts simulated clock cycles, but its architectural trace records only PC/ACC/AR0/AR1 and includes no PUSH/POP transcript or pin phases. The new strict capture classifier qualifies only the H1/H2/H3 measurement workflow and cannot supply missing hardware data | authoritative original TI PUSH/POP control waveform, or the reproducible original-NMOS pin capture defined in `docs/research/push_pop_bus_experiment.md`; measure address/data and `MEN`/`WE`/`DEN` on both intervals, retain 32 runs plus raw/photo/image hashes, and pass `tools.trace.push_pop_capture` review-readiness checks | blocks honest native-phase RTL qualification even though model/tool, MAME functional comparison, stack-state research, and capture-tool qualification can proceed |
| OQ-017 | At what subphase does `SUBC` make its ACC result available, and what deterministic silicon behavior occurs if the immediately following instruction violates TI's rule that it cannot use ACC? | RESEARCHING/CORROBORATED RELATED-EMBODIMENT: both original production guides state the scheduling prohibition but do not describe a violation. TI's 1982 simulator documents stop code 9950 for accumulator use in the first clock cycle after SUBC, corroborating active enforcement in contemporary reference software but revealing no physical violating result. Contemporary TI patent US4577282A stages the unshifted ALU result through Q4/Q1/Q2 and performs an accumulator-local shift at Q3 of a following non-ALU/NOP state, explaining the restriction, but calls its SUBC two-state rather than the production one-cycle count. Pinned IKA also delays the result but is not a production oracle. The current implementation commits final ACC at its instruction boundary and claims only legal streams. The strict `tools.trace.subc_capture` workflow preserves the first physical export without an expected value, accepts stable unanticipated words, and requires the legal comparator plus exact image/raw/photo provenance; it has no capture data and cannot assign the missing subphase | original pipeline documentation, or the stable violating-SACL versus NOP-separated original-NMOS capture defined in `docs/research/subc_pipeline_experiment.md`; its first export deliberately has no expected result and must pass capture-package review without automatic confidence promotion | may change deliberately invalid code and the exact interrupt/result-visibility boundary; legal Hard Drivin' code must be audited for the required gap |
| OQ-018 | Which `SUBC` arithmetic stage drives sticky `OV` on the original TMS32010: the intermediate subtraction, the final left shift, or both? | PROVISIONAL, NARROWED: SPRU001B defines both stages but omits the source; later first-generation TI documentation says SUBC affects OV, ignores OVM, and never saturates. The related patent derives status from the ALU path before an accumulator-local final shift, supporting the current intermediate-only hypothesis, but is not a production specification. Pinned IKA instead flags the later result, while pinned MAME's apparent intermediate check cannot set OV. The strict physical-capture workflow now reads OV from primary-defined SST bit 15, validates every other known fixture status bit except separately disputed reserved bit 1, preserves all four outcome pairs, and requires 32 consistent provenance-checked runs. Synthetic tests are not device evidence | original-part erratum/microarchitecture detail, or the stable original-NMOS two-vector status capture in `docs/research/subc_pipeline_experiment.md` that separates intermediate-only from final-shift-only overflow and passes raw-capture review | affects status saves and BV after out-of-domain SUBC operands; normal documented positive divide use does not exercise these boundary cases |
| OQ-019 | If `DINT` is the already-pipelined instruction immediately before an otherwise ready interrupt acknowledge, does its INTM write cancel entry or does the internal interrupt processor complete entry? | RESEARCHING/CONFLICT (`SC-039`): current model/RTL cancels entry and retains the pending request. Original Figure 2-11's mode gate and assembly-guide inhibition wording support that hypothesis, but no original source orders DINT retirement against grant at the Figure 2-12 N+1 boundary. SPRU001B's isolated claim that set INTM validates active is an internal polarity typo contradicted by its diagram and disable prose, not race evidence. Later SPRU013 says DINT masks immediately after execution but its different Figure 3-20 dummy-fetches N+1 instead of executing it. MAME lacks the overlapping boundary; pinned IKA evaluates old-mask `int_rq` while DINT sets the mask and therefore represents entry-wins. The strict `tools.trace.dint_interrupt_capture` workflow now preserves all port words, independently checks exact image/anchors, recomputes the 50 ns setup, one-local-CLKOUT low width, 15 ns fall-time limit, sampled INT agreement, and requires 32 stable runs plus raw/photo/calibration hashes. Synthetic candidates are workflow tests, not device evidence | original-part microarchitecture/erratum wording or the stable original-NMOS pulse/address/stacked-PC experiment in `docs/research/dint_interrupt_race_experiment.md`; repository fixture has no expected hardware sequence and classifier `review_ready` cannot promote confidence without raw-capture review | low for normal handlers; relevant to deliberately masking at the last recognition boundary |
| OQ-020 | Should a digital Hard Drivin' audio adapter complement DAC code bit 11? | RESEARCHING/CONFLICT (`SC-019`): A044427 Rev A directly wires `TD15:TD4` to Am6012 `B1:B12`; its positive-reference, grounded-complement, inverting-current, and AC-coupled path is monotonic straight binary and contains no MSB inverter. Historical MAME audit shows its signed-word XOR began with the first located sound support in MAME 0.62 (2002); the schematic-inversion comment appeared only in the 2016 AM6012 migration that preserved the old software interpretation and modeled symmetric references. Thus MAME corroborates intended signed rendering only as a software lineage, not as independent pin evidence. No authenticated alternate drawing/ECO was located | execute `docs/research/hard_drivin_dac_code_audit.md`: first use TM-327 `320 DAC Ones`/`320 DAC Ramp` to capture `/DACL`, TD15, 75E D/Q, Am6012 B1/IOUT, DACOUT, and both sides of C26 on a fully photographed board; then correlate authorized normal-game port-0 writes to analog output and repeat on a second production board | raw FPGA port adapter remains `data[15:4]`; MAME transform remains separately named. A default signed renderer or production-equivalence claim is blocked, but the processor and physical latch are not |
| OQ-021 | Does every Driver Sound 68000 firmware revision assert `/320RES` for the complete host program-RAM or upper-Y5 direct-I/O access interval, and how long does it wait before/after release? | RESEARCHING: A044427 proves the host address/data LS244/LS245 enables and TMS operation are independent; overlap can contend in program RAM or on the direct TMS I/O/data nodes. Pinned MAME does not test the contract: shared RAM and DSP I/O are always host-accessible and latch bit 4 drives HALT rather than the physical reset/buffer sequence | authorized 68000 ROM trace/disassembly, Atari initialization documentation, or captures of `/320RES`, `/320RAM`, `/RAMCE`, `/RAMWR`, `/PDEN`, `/PWE`, and TMS strobes during boot/reload and direct I/O | a MiSTer wrapper must enforce or report the required ownership interval; the current direct-I/O composition suppresses/reports active host/TMS I/O overlap instead of inventing priority |
| OQ-022 | Does any Driver Sound 68000 firmware perform byte writes to the TMS32010 program-RAM window, and what complete 16-bit value reaches the four SRAM slices if it does? | PARTIALLY_RESOLVED_PRIMARY (`SC-022`): A044427 routes A12:A1, all D15:D0 bits, and one `/RAMWR` to all four SRAM slices without UDS/LDS lane enables. Original-MC68000 Table 3-1 establishes that a selected byte is driven on both bus halves, so every slice captures `{byte, byte}`. Pinned MAME's `COMBINE_DATA` merge conflicts with that physical result | authorized 68000 ROM trace/disassembly for actual access widths; physical timing capture only if `/RAMWR`, data setup/hold, or substituted-68k behavior matters | the timing-derived lower-Y5 path normalizes original-MC68000 bytes before the complete-word storage callback and reports each accepted byte transfer; the external callback remains an already captured complete-word contract |
| OQ-023 | What loaded A044427 hardware consumes the TMS32010 port-3 `/CPORT` strobe? | RESOLVED_PRIMARY: Rev-A sheet 4 LS374 `50L` captures `TD7:TD0` on `/CPORT`; `/320PORT` enables its true outputs onto host `D15:D8`. The latch has no drawn clear. Pinned MAME labels the write `COM port TD0-7` but only logs it | closed by A044427 Rev A sheet 4, PDF pp. 7-8, and TI SDLS165B LS374 behavior; physical testing only if propagation or power-up state matters | the wrapper must retain an eight-bit TMS-to-host latch with explicit invalid power-up state; `OQ-030` separately covers the undriven host byte |
| OQ-024 | Does Driver Sound firmware perform byte writes to the 512-word communication RAM, and what full word reaches both HM6116 devices if it does? | PARTIALLY_RESOLVED_PRIMARY (`SC-025`): A044427 supplies one `/CRWE` to both byte-wide RAMs and does not route `/HEU` or `/HEL` into the RAM controls. Original-MC68000 Table 3-1 establishes that a selected byte is driven on both bus halves, so both devices capture `{byte, byte}`. Pinned MAME's `COMBINE_DATA` merge conflicts with that physical result | authorized 68000 ROM trace/disassembly for actual access widths; physical timing capture only if `/CRWE`, data setup/hold, or substituted-68k behavior matters | the timing-derived Y6 path normalizes original-MC68000 bytes before the complete-word storage callback and reports each accepted byte transfer; the external callback remains an already captured complete-word contract |
| OQ-025 | Does every Driver Sound firmware revision guarantee that the TMS32010 never reads port 1 while `CRAMEN=1` gives the communication RAM to the 68000? | RESEARCHING/CONFLICT (`SC-023`): A044427 disables the CRD-to-TDI buffer in host mode, so the RAM word is not a defined DSP response; pinned MAME returns it regardless of `CRAMEN` | authorized DSP/68000 ROM traces, Atari protocol documentation, or physical captures of CRAMEN, DEN, port address, TDI, and host selection | a digital wrapper should hold or report the out-of-contract read rather than silently grant both sides |
| OQ-026 | Which sound-ROM blocks are populated on each Driver Sound revision, and what reaches TDI if software selects an absent block? | PARTIALLY_RESOLVED_PRIMARY (`SC-044`): A044427 maps A-row `65A..5A` to blocks 0-5, C-row `65C..5C` to blocks 6-11, marks the complete C row `NOT LOADED`, and leaves 12-15 undecoded. TM-356 provides primary upgrade evidence: `A046491-02` receives `136052-3125` at `45A`/block 2 when needed and new `136077-1017` at `45C`/block 8. Pinned MAME instead packs the 45C file at block 4. Exact factory/other-variant population remains unknown. With no selected/populated ROM, shown outputs do not drive `SD14:SD7`; MAME's zero is not hardware evidence | follow `docs/research/hard_drivin_sample_rom_population_audit.md`: board/revision-labelled photographs, socket-to-select continuity, repeated authorized device reads through the socket-based hash tool, and port-6/port-0 traces; capture `SD14:SD7`/TDI only if an absent selection matters | keep `sound_rom_present_i[11:0]` sparse; physical 45C sets bit 8, never packed bit 4. Reject absent/undecoded selections without claiming an open-bus value |
| OQ-027 | Does any populated Driver Sound revision use port-4 `MUTE`, and what audio semantics does it implement? | UNKNOWN/CONFLICT (`SC-027`): Rev-A clocks TD0 into LS74 Q and names complementary `/Q` as `MUTE`, so D0=1 drives that net low; its only drawn 4066B consumer is explicitly `NOT LOADED`. Pinned MAME says D0=1 mutes but implements no effect | another loaded board revision/ECO or physical inspection and signal/audio capture showing the consumer and polarity | the board wrapper may expose the raw complement state; it must not gate audio or claim functional mute behavior yet |
| OQ-028 | What exact `/BIOS` phase follows power-up/reset release, and what does LS74 70S produce when an independent 1 MHz `/320BIO` transition violates CLKOUT setup/hold? | PARTIALLY RESOLVED_PRIMARY (`SC-028`): two LS161s preload `0xce`, count through `0xff`, and produce a one-1-MHz-period low pulse every 50 periods; `/RESET` clears the source LS74 only, while neither counter nor the CLKOUT resampler has a board-reset input. The 16 MHz-derived 1 MHz source and 20 MHz-derived CLKOUT use independent crystals | simultaneous physical capture of 1 MHz, counter RCO, `/320BIO`, CLKOUT, and `/BIOS` across power-up/reset release and drifting edge alignments, or a board qualification specification | the standalone FPGA model requires noncoincident enables, exports phase/data validity, and never claims its caller-supplied deterministic seed is physical power-up state |
| OQ-029 | What sixteen-bit value does a production A044427 Rev-A board present during a TMS32010 port-2 `/CMPRD` read? | UNKNOWN/CONFLICT (`SC-029`): sheet 3 routes only `CMPOUT` through LS244 10H to `TDI15`, with no compare-target connections to `TDI14:TDI0`; sheet 8's microphone/LM311 source and its `R11` pull-up are explicitly `THIS SHEET NOT LOADED.` Pinned MAME instead returns a complete zero word | production BOM/assembly evidence plus simultaneous physical capture of `/CMPRD`, `CMPOUT`, and `TDI15:TDI0`, or a populated board-variant/ECO schematic that defines every returned bit | keep port 2 on an explicit external callback; a synthetic zero may be a test sentinel but must not be labeled Rev-A hardware behavior |
| OQ-030 | What complete 68000 word is observed in host lanes not driven by the selected partial read target? | UNKNOWN: A044427 `/320PORT` drives only `D15:D8`; `/SWITCHES` and `/READSTAT` each drive only `D15:D12`; upper-Y5 direct `/CMPRD` qualifies only TMS/host bit 15; and direct read port 3 has no drawn source. No selected-target source is drawn for the remaining lanes. Pinned MAME returns deterministic values, but those are abstractions | physical bus captures, a complete board-level pull/bus-hold analysis, or an Atari hardware contract specifying open-bus behavior | FPGA host reads must expose driven and valid masks or apply an explicitly platform-defined open-bus policy; they must not label zero-filled complete words as verified board behavior |
| OQ-031 | Do authorized main/sound firmware use byte writes at either A044427 mailbox, and what flag result occurs when write-preset releases at the opposite bus's read-clock edge? | PARTIALLY_RESOLVED_PRIMARY (`SC-031`): A044427 LS138 `20P` generates main `/MAINWR` locally at `0x840000..0x843fff`; neither that path nor local `/SOUNDWR` uses the available byte enables. Original-MC68000 Table 3-1 documents that a selected byte appears on both halves of `D15:D0`, so either mailbox captures `{byte, byte}`. The LS74 function table proves preset dominance while low and declares simultaneous preset/reset clear invalid, but gives no exact result for a read edge at preset release. Pinned MAME's local byte merge conflicts with the primary hardware result | authorized main/sound firmware access-width audit; for collision closure, identified-board captures of `/MAINWR`, `/SOUNDRD`, `/SOUNDWR`, `/MAINRD`, reset, and both flags around targeted preset-release/read-edge tests | `hard_drivin_mc68000_write_word` implements original-MC68000 word and duplicated-byte capture for the timed local path; the main callback remains an already captured complete-word contract. Same-callback coincident flag operations remain explicitly invalid as a conservative release-edge abstraction, not a claim that every overlap is unknown |
| OQ-032 | Are Driver Sound connector inputs `J3-11`, `J3-9`, `J3-8`, and `J3-7` connected in any production cabinet, and what voltage/read value does an unconnected input produce? | PARTIALLY RESOLVED_PRIMARY: A044427 Rev A maps the four inputs non-inverting to `/SWITCHES` `D15:D12`, ties only J3-1/J3-2 to ground, and gives each signal a 1 kOhm series resistor plus capacitive shunts but no DC pull. SP-327 Hard Drivin' cockpit and SP-360 Race Drivin' compact main wiring both show their `044427-XX` Sound PCB power/audio harnesses but no J3 cable or cabinet device. Thus those published configurations assign no cabinet functions. TI specifies valid driven LS244 thresholds/current but not an open-input logic state; MAME's fixed zero is still not hardware evidence (`SC-033`) | inspect and continuity-map J3, cables, jumpers, and rework on documented `A046491-01` and `-02` boards; then capture all four external/input voltages and local-read `D15:D12` with J3 disconnected and in every populated contact state, following `docs/research/hard_drivin_switch_input_audit.md` | the raw FPGA mapper remains valid for optional/measured uses. A platform reproducing either reviewed cabinet should clear all four source-valid bits, not force zero or one; other assemblies and the physical floating value remain unknown |
| OQ-033 | What electrical timing margin and power-up transient apply to the A044427 `RVA`/`/DTACK`/`/RVAS` generator? | PARTIALLY RESOLVED_PRIMARY: A044427 plus Motorola's MC68000 bus states establish the complete logical S2-through-S7 sequence. `/AS` sets F74 `40R`; the next 8 MHz rise asserts a one-period `RVA` and `/DTACK`; falling-edge F74 `50S` holds `/RVAS` active through the S7 data-latch edge. The path has no READY input or held-`/AS` re-arm, and `/RVF` separately qualifies LS138 `30N`. `PR4`/`PR5` are pull-ups, so the F74 state is not board-reset. Complete F04/F74/LS20/LS08 propagation/loading margin and any power-up select transient have not been calculated or measured | period-correct component AC data for the populated families plus board loading/trace budget, or simultaneous capture of 8 MHz, `/AS`, `RVA`, `/DTACK`, `/RVAS`, and one decoded strobe across power-up and ordinary reads | the same-clock FPGA adapter and opt-in board composition implement the qualified edge sequence with deterministic idle initialization only as an explicit platform convention; they expose no arbitrary callback stalls and do not claim raw-pin CDC, nanosecond, or power-up equivalence |
| OQ-034 | Which `E1/E2` EPROM strap and device capacity are installed on each production Driver Sound revision? | PARTIALLY_RESOLVED_PRIMARY (`SC-034`): A044427 Rev A draws two `27256D20` devices and alternative common-pin-1 links: E1 to +5 V, E2 to CPU A16. Contemporaneous AMD data identifies pin 1 as VPP on a 32Kx8 27256 and A15 on a 64Kx8 27512; 27256 read requires VPP=VCC. Thus E1 is the required drawn-27256 choice (64 KiB combined, CPU A15:A1) and E2 is the intended 27512 choice (128 KiB, CPU A16:A1). TM-356 now proves the prescribed Race Drivin' deluxe-cockpit upgrade on `A046491-02`: install `136077-1032`/`1033` and move the jumper to E2. It does not prove factory population, successful field work, or unused halves. Pinned released-game declarations remain 0x8000 bytes/lane; the Panorama prototype is 0x10000/lane | obtain an assembly/BOM/ECO option table or follow `docs/research/hard_drivin_program_rom_strap_audit.md`: board-identified photographs/continuity plus repeatable authorized ROM reads analyzed by `tools.reference.hard_drivin_program_roms`; distinct upper/lower 32 KiB halves prove A16 is information-bearing but file size alone never proves a physical strap | retain the A15:A1 drawing-default callback for Hard Drivin'; model TM-356's E2 upgrade only as an explicit tested Race Drivin' wrapper mode. Do not treat the legacy `populated_` signal name or a field instruction as inspection evidence |
| OQ-035 | What complete RESET/HALT startup and release contract should a future FPGA local-MC68000 integration use? | PARTIALLY RESOLVED_PRIMARY (`SC-035`): A044427 sheet 2 proves `/MRES && /SRES` drives active-low A of LS123 `100N`, whose 47 kΩ/10 µF network has about 155.1 ms nominal typical duration; TI's early-trigger rule gives about 2.2 ms of trigger inhibit for 10 µF; active-low `SOUND.RESET` and `/Q` then drive separate 7406/pull-up branches with equal stable logic to MC68000 RESET and HALT. Motorola §5.5 requires paired assertion. A parameterized tick-domain source and separate SRAM-scrub interlock are verified, but installed-component tolerance, analog power-up and exact trigger-boundary behavior, raw-input CDC, firmware use, and the future 68000-core reset interface are not | production BOM/component tolerances or physical RESET/HALT capture across power-up and both trigger sources; authorized firmware/startup audit; selected FPGA 68000-core reset specification; calibrated hold/inhibit ticks and cross-domain release analysis | keep RESET/HALT as separate observable outputs; only select the common digital source after the platform supplies a qualified tick/CDC policy, then apply the explicit scrub interlock; do not call nominal tick behavior pin-timing equivalence |
| OQ-036 | What drives SP-327 main-board `/RESET`, and what complete timing/CDC contract should qualify main `/AS`, `/DTACK`, `/RVAS0`, and `/RVAS` in an FPGA composition? | PARTIALLY RESOLVED_PRIMARY (`SC-036`): SP-327 sheet 7 proves `/MRES` is a permanently enabled LS244 copy of `/RESET`, and sheets 4/7 plus A044427 sheet 1 prove the write-only `0x84c000..0x84ffff` `/SRES` mirror. Sheet 4 resolves the `/AS`-qualified `A23:A21` primary LS138, `/RAMEN` and `/RHSBUS` `A15:A14` LS139 subdecoders, both hold states, and the complete combinational `/DTACK` cone. `/AS` is captured; normal high-phase S2 assertion presets early `/RVAS0` on S3 falling; S4 rising creates one-period `RVA` and presets `/RVAS`; and the sampled `/DTACK` low-to-high clock releases both D=0 F74s unless active `/RVAS0` preset has priority. A044425 Rev-J supplemental sheets 10 and 15 resolve `/GSPWAIT` and `/MSPWAIT` as the direct `HRDY` outputs of the corresponding TMS34010 host interfaces; TI SPVU001 defines their ready/wait contract. SP-327 sheet 6 and Motorola ADI988R1 resolve `/RDUART` as MC68681 `CS`, `/DUDTACK` as its pulled-up active-low open-drain `DTACK`, and 3.6864 MHz X1-clock recognition whose setup miss may defer acknowledgement one device cycle. The common-clock-event blocks are now composed and address-driven, but the reviewed material still does not reveal the original `/RESET` driver, workload-specific TMS34010 wait duration, exact 8 MHz-to-3.6864 MHz phase for a particular DUART access, component/board propagation margin, raw-pin CDC, or unreset power-up state | Atari system/power/reset interconnect or complete net-source drawing; selected FPGA event/CDC architecture; physical capture of 8 MHz, 3.6864 MHz, `/AS`, `/S4`, `RVA`, `/RVAS0`, both TMS34010 `HCS`/`HRDY` pairs, MC68681 `CS`/`DTACK`, main `/DTACK`, `/RVAS`, `/EXTBUS`, `/SRES`, and `/MRES`; populated logic-family/loading timing calculation | use `hard_drivin_main_bus_control` only behind a qualified level/event adapter; accept system reset and peripheral-owned ready inputs explicitly; do not synthesize TMS34010 or MC68681 latency policy into the hierarchy or infer held-strobe release from elapsed time/a generic transaction commit, because a missing sampled `/DTACK` low leaves both D=0 F74s held |
